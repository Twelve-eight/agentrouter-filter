node_modules/
*.log
.tmp/

# Per-machine credentials (upstream API keys). Never committed.
.env.local

# Runtime usage accounting (per-request JSONL). Not source: it grows with use and
# is machine-local. Kept on disk for the /stats dashboard.
data/
